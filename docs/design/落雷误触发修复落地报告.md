# 落雷误触发修复落地报告

- 落地日期：2026-08-12
- 项目：rougelike_crew（Godot 4.7.1）
- 依据：docs/design/落雷误触发排查报告.md（2026-08-11，9 条误触发路径）
- 范围：P0 全部 + P1 全部 + P2 全部 + 法杖强化显示与商店关闭

## 一、修复清单逐条状态

| # | 路径 | 位置 | 状态 | 说明 |
|---|------|------|------|------|
| P0-1 | 高 DPS 档位随机落雷 | scripts/fx/fx_manager.gd:505 | 修 | 加 `_school_holdings_of("lightning") > 0` 门控：无雷系构筑时档位 3 只发 gold 粒子雨，不再随机落雷；持有雷系后恢复爽感落雷。该改动在会话开始前已由 Beauvoir 轮次写入工作区（含修复注释），本次全量验证通过 |
| P0-2 | Boss 转阶段全屏雷爆 + 雷系真伤 | scripts/enemies/boss.gd:770-799 | 修 | 视觉改金色敌技系：Boss 位/玩家位/场地四角共 6 处 `gold` 爆 + 震屏；清屏冲击波元素 `lightning`→`void`（敌技语义，不再与玩家雷系视觉/水M4 导电联动产生敌我混淆）。已随提交 49322ec 入库，本次验证 |
| P0-3 | Boss 四技能复用玩家雷视觉 | scripts/enemies/boss.gd（sweep/beam/whirl/shield） | 修 | sweep/beam/whirl 的连续特效 `lightning`→`fire`（红色系），shield `lightning`→`buff`；仅视觉，伤害逻辑（player_hit）未动。另 Boss 死亡双爆 `fire+lightning`→`fire+gold` 一并修正。已随提交 49322ec 入库 |
| P1-4 | 切关中心落雷 | scenes/game/game_root.gd:473 | 修 | 改用 `_top_school()` 按主流派（school_holdings 持有数最高者）取色；无任何构筑时默认 `fire`。已随提交入库，本次验证 |
| P1-5 | 法术部件三选一无元素过滤（诱导路径） | scripts/core/game_state.gd `_make_spell_choice` | 修（本次加固） | 已提交版本有元素门控但存在漏洞：8 次重抽全部抽到未持有元素时会**回退最后一个随机核心**，无雷/仅火构筑仍可能漏出闪电核心。本次改为**预过滤核心池**：`element=="" 或 "blade" 或 holdings.has(el)` 才进池，空池直接返回空选项——未持有元素的核心绝不进入生成池（与 N2 物品过滤同语义）。仅改此函数，与 Ptolemy 的升级概率改动（_element_weight）无冲突 |
| P1-6 | 精英三连射/狙击手蓄力雷光 | scripts/enemies/enemy.gd（triple/sniper） | 修（他方工作区改动，本次未触碰） | 蓄力/施法闪光已改 `fire` 红色系（enemy.gd:564/944 带修复注释）。文件正在被动画代理并发修改（87 行未提交改动），按任务文件边界不触碰；剩余 `lightning` 引用均为**受电击状态视觉**（玩家雷系命中后的正常反馈，有触发门控），非误触发 |
| P2-7 | 拾取任意法术部件闪雷 | scripts/items/item_pickup.gd:73-75 | 修 | 拾取特效按核心元素取色：解析 payload 的 core → `GameState.spell_core_element()`，命中元素用对应色（如 fireball→fire），无元素/未知回退 `gold`。已随提交入库 |
| P2-8 | 防御 M10 金身雷爆 | scripts/synergies/defense_synergy.gd:354 | 修 | 金身触发特效 `lightning`→`gold`（金色 buff 视觉）。已随提交入库 |
| P2-9 | 召唤物精英化雷爆 | scripts/synergies/summon_synergy.gd:215 | 修 | 精英化特效 `lightning`→`gold`（召唤/金色系）。工作区未提交改动，本次验证 |

## 二、法杖强化显示与商店关闭（wand_shop.gd）

1. **强化效果显示**：强化页槽位卡新增「当前等级 + 伤害加成/新数值」行（如 `Lv.1 ｜ 伤害 +8%（×1.08）`，未强化显示 `未强化`）；强化按钮文本随等级更新（`强化 Lv.1（350金）`）。
2. **强化成功提示**：`_upgrade_wand` 成功后显示金色横幅「强化成功 +8%！伤害 ×1.08（Lv.1）」（UiTheme.GOLD 粗体），数值取自 `GameState.WAND_UPGRADE_BONUS`（0.08/级）与强化后等级，与 spell_caster.gd:287 伤害公式一致。
3. **强化完成关闭商店**：强化成功后卡片/横幅即时更新，**1s 展示期后自动 `_close_shop()`**（与购买法杖行为一致，进入下一关）；`_finish_enhance_close` 通过 timer 信号连接调用，节点释放自动断连、商店已被其他路径关闭时跳过，无重入风险。
4. **入口确认**：选择页「强化法杖」按钮 → `_goto_enhance`（强化页），入口逻辑正确，由 test_wand_shop_flow 覆盖（PC/手机双端）。
5. 冲突说明：Ptolemy 正在同文件改按钮顺序/Brotato 改造（RARITY_WEIGHT、商品卡网格、锁卡、构筑单购等），已用 git diff 核对——本次仅动 `_enhance_banner`、`_build_enhance_page`、`_goto_enhance`、`show_shop`、`_refresh`（槽位卡）、`_upgrade_wand`、`_finish_enhance_close`，与对方 hunk 零重叠。

## 三、改动文件清单

本次会话实际修改（文件边界内）：

| 文件 | 改动 |
|------|------|
| scripts/core/game_state.gd | `_make_spell_choice` 元素门控加固（预过滤核心池，仅此函数） |
| scripts/ui/game/wand_shop.gd | 强化页金色横幅、槽位卡等级/伤害显示、强化成功展示 1s 后 `_close_shop()` |
| scripts/tests/test_no_lightning_leak.gd | 新增回归测试（见下） |

确认既有修复（本会话未改动，已逐条验证）：

| 文件 | 内容 | 状态 |
|------|------|------|
| scripts/fx/fx_manager.gd | P0-1 高 DPS 落雷门控 | 工作区未提交（Beauvoir） |
| scripts/enemies/boss.gd | P0-2/P0-3 转阶段+四技能+死亡爆 | 已提交（49322ec） |
| scenes/game/game_root.gd | P1-4 切关主流派取色 | 已提交 |
| scripts/items/item_pickup.gd | P2-7 拾取按元素取色 | 已提交 |
| scripts/synergies/defense_synergy.gd | P2-8 金身改金色 | 已提交 |
| scripts/synergies/summon_synergy.gd | P2-9 精英化改金色 | 工作区未提交 |
| scripts/enemies/enemy.gd | P1-6 蓄力/三连射改红色系 | 工作区未提交（动画代理并发区，未触碰） |

## 四、测试结果（全部 headless 运行，Godot 4.7.1 console + TEMP/APPDATA 重定向到 .tmp）

### 新增 test_no_lightning_leak.gd —— NO LIGHTNING LEAK ALL PASS（退出码 0）

运行：`godot --headless --path . -s res://scripts/tests/test_no_lightning_leak.gd`

1. ① 高 DPS 门控：空构筑（无雷 holdings）强制档位 3 → 只发 `gold` 不发 `lightning`；持有 thunder_1 后同档位恢复 `lightning`（正向对照）。
2. ② 三选一过滤：空构筑/仅火系构筑各 60 次 `_make_spell_choice` → 从未出现 lightning 核心；持有闪电核心后必出（正向对照）。
3. ③ Boss 转阶段：直接驱动 `_on_phase_up()`（skeleton_king）→ 记录 EventBus.fx_explosion，无 `lightning` kind，含 `gold`。
4. ④ 强化：`_upgrade_wand(0)` 后等级=1、金币按 250 扣减、伤害倍率=1.08、estimate_dps 提升、金色横幅可见且含「强化成功」、卡片按钮更新为「强化 Lv.1」，随后 1s 内商店自动关闭（轮询断言）。

### 回归（既有测试未破坏）

| 测试 | 结果 |
|------|------|
| scripts/tests/smoke_test.gd | **SMOKE OK**（heal_pct==0.10 断言完好，数据表/场景/编译全部通过） |
| scripts/tests/test_wand_shop_flow.gd | **WAND SHOP FLOW ALL PASS**（含 autoplay 强化路径——强化后 1s 关店与替换购买路径互不干扰） |
| scripts/tests/test_lightning_fx.tscn | **LIGHTNING FX ALL PASS**（雷系真实触发的视觉/链跳/麻痹机制无回归） |

注：headless 输出中的 "Failed to read the root certificate store" 为 Windows 无头 TLS 初始化噪音，所有测试（含 smoke）均出现，与本次改动无关。

## 五、边界与遗留说明

1. **enemy.gd（P1-6）**：修复存在于动画代理的未提交工作区（triple/sniper 蓄力已改 fire）。若其改动被丢弃/回退，需重做两处（enemy.gd:564/944）；当前工作区状态无 lightning 视觉残留（剩余 lightning 引用为受电击状态视觉）。
2. **thunder_synergy.gd 全部不动**：所有雷系触发源已有 holdings 门控，无门槛触发不存在（排查报告 §2.4）。
3. **Boss 转阶段真伤**：元素已从 lightning 改为 void，避免触发玩家水M4 导电等雷系联动；未引入 attacker 来源标记（系统暂无该上下文，报告 §五 建议留作后续）。
4. **高 DPS 震屏**：lightning 震屏分量随 lightning holdings 缩放保留（无雷时仅基础小震屏，非雷视觉，符合预期）。
5. **强化关店时机**：采用「展示 1s 后关闭」而非立即关闭，保证金色横幅与卡片数值玩家可见（与"强化完成即关闭"兼容；期间商店仍可手动关闭/返回）。
6. **文件边界遵守**：未触碰 scripts/enemies/enemy.gd、scripts/ui/ 其他文件、data/enemies.json、.git；game_state.gd 仅改 _make_spell_choice，wand_shop.gd 仅改强化相关函数，均与并发代理改动无重叠。
